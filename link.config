MEMORY {
  INIT: 
    start = $0fff
    size = $0e;
  RAM:  
    start = $100d
    size = $0bf3
    fill = yes;
  CHARMEM:
    start = $1c00
    size = $200
    fill = no;
}

SEGMENTS {
    HEADER: load = INIT;
    BOOT:   load = RAM;
    CODE:   load = RAM;
    CHARS:  load  = CHARMEM;
}
